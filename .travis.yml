language: python

python:
  - "3.5"

install:
  - cp passwords.example.py passwords.py
  - "pip install --upgrade pip"
  - "pip install --trusted-host content.dev.faforever.com -r requirements.txt"

before_script:
  - "mysql -e 'create database faf_test;'"

script:
  - pip install -e .
  - export DB_ENV_MYSQL_ROOT_PASSWORD=
  - py.test --slow --cov-report term-missing --cov=server
  - pyflakes . || true

after_success:
  - coveralls
