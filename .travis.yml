language: python

python:
  - "3.4"

install:
  - "sh ./.travis.sh"
  - "pip install flexmock pytest pytest-qt pytest-cov python-coveralls configobj mock pyside"

script:
  - py.test --cov . --cov-report term-missing

after_success:
  - coveralls
